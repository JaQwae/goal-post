<body>
    <div class="wrapper mypost">

    <header class="hero">
        <h2>Your Posts</h2>
    </header>

{{!-- <div class="card text-center single-post"> --}}
    {{!-- <div class="card"> --}}
    <div>
        {{!-- <div class="card"> --}}
            <div class="link single-post">

                {{#if posts.length}}
                <section class="flex-row justify-center">
                    {{#each posts as |post|}}
                    {{> singlemypost post}}
                    {{/each}}
                </section>
                {{/if}}
            </div>
        {{!-- </div> --}}
    </div>

<div class="container">
    <button type="submit" class="btn" id="create-btn"><a class="link" href="mypost/new">+ New Post</a></button>
</div>



    </div>
</body>